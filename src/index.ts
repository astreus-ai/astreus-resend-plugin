import { ResendPlugin } from './plugin';
import { 
  ResendConfig, 
  EmailMessageOptions, 
  TemplateEmailOptions,
  EmailResponse,
  EmailAttachment,
  EmailTag
} from './types';

// Export the plugin as default export
export default ResendPlugin;

// Export all types
export {
  ResendConfig,
  EmailMessageOptions,
  TemplateEmailOptions,
  EmailResponse,
  EmailAttachment,
  EmailTag
}; 